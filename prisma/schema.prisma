generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model announcement {
  id                                String   @id
  content                           String   @db.Text
  isActive                          Boolean  @default(true)
  speed                             Int      @default(12)
  creatorId                         String
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime
  user_announcement_creatorIdTouser user     @relation("announcement_creatorIdTouser", fields: [creatorId], references: [id], map: "Announcement_creatorId_fkey")
  user_targetedusers                user[]   @relation("targetedusers")

  @@index([creatorId], map: "Announcement_creatorId_fkey")
}

model attendance {
  id          String   @id
  userId      String
  date        DateTime
  clockInEnc  String?  @db.Text
  clockOutEnc String?  @db.Text
  isHoliday   Boolean  @default(false)
  statusEnc   String?  @db.Text
  notesEnc    String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime
  user        user     @relation(fields: [userId], references: [id], onDelete: Cascade, map: "Attendance_userId_fkey")

  @@unique([userId, date], map: "Attendance_userId_date_key")
}

model category {
  id        String   @id
  name      String   @unique(map: "Category_name_key")
  createdAt DateTime @default(now())
  updatedAt DateTime
  recipe    recipe[]
}

model chatMessage {
  @@map("chatmessage")
  id         String   @id
  chatRoomId String
  senderId   String
  content    String   @db.Text
  createdAt  DateTime @default(now())
  chatroom   chatRoom @relation(fields: [chatRoomId], references: [id], onDelete: Cascade, map: "ChatMessage_chatRoomId_fkey")
  user       user     @relation(fields: [senderId], references: [id], onDelete: Cascade, map: "ChatMessage_senderId_fkey")

  @@index([chatRoomId], map: "ChatMessage_chatRoomId_idx")
  @@index([senderId], map: "ChatMessage_senderId_idx")
}

model chatParticipant {
  @@map("chatparticipant")
  id         String   @id
  chatRoomId String
  userId     String
  lastSeenAt DateTime @default(now())
  createdAt  DateTime @default(now())
  chatroom   chatRoom @relation(fields: [chatRoomId], references: [id], onDelete: Cascade, map: "ChatParticipant_chatRoomId_fkey")
  user       user     @relation(fields: [userId], references: [id], onDelete: Cascade, map: "ChatParticipant_userId_fkey")

  @@unique([chatRoomId, userId], map: "ChatParticipant_chatRoomId_userId_key")
  @@index([chatRoomId], map: "ChatParticipant_chatRoomId_idx")
  @@index([userId], map: "ChatParticipant_userId_idx")
}

model chatRoom {
  @@map("chatroom")
  id              String            @id
  name            String?
  isGroup         Boolean           @default(false)
  createdAt       DateTime          @default(now())
  updatedAt       DateTime
  chatmessage     chatMessage[]
  chatparticipant chatParticipant[]
}

model customWorkSchedule {
  @@map("customworkschedule")
  id        String   @id
  startDate DateTime
  endDate   DateTime
  startTime String
  endTime   String
  reason    String
  createdAt DateTime @default(now())
  updatedAt DateTime
}

model hRDocument {
  @@map("hrdocument")
  id          String   @id
  name        String
  link        String?  @db.Text
  filePath    String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime
  description String?  @db.Text
}

model logActivity {
  @@map("logactivity")
  id        String   @id
  userId    String
  date      DateTime
  activity  String   @db.Text
  problem   String?  @db.Text
  image     String?
  createdAt DateTime @default(now())
  updatedAt DateTime
  user      user     @relation(fields: [userId], references: [id], onDelete: Cascade, map: "LogActivity_userId_fkey")

  @@unique([userId, date], map: "LogActivity_userId_date_key")
}

model machineAsset {
  @@map("machineasset")
  id            String    @id
  name          String
  code          String?
  specification String?   @db.Text
  location      String
  price         Float?
  notes         String?   @db.Text
  image         String?
  purchaseDate  DateTime?
  usefulLife    Int?
  residualValue Float?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime
}

model mOU {
  @@map("mou")
  id          String   @id
  date        DateTime @default(now())
  number      String
  name        String
  institution String
  content     String   @db.Text
  link        String?  @db.Text
  filePath    String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime
}

model offerLetter {
  @@map("offerletter")
  id          String   @id
  date        DateTime @default(now())
  number      String
  name        String
  institution String
  content     String   @db.Text
  link        String?  @db.Text
  filePath    String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime
}

model order {
  id          String      @id
  orderNumber String      @unique(map: "Order_orderNumber_key")
  userId      String?
  createdAt   DateTime    @default(now())
  user        user?       @relation(fields: [userId], references: [id], map: "Order_userId_fkey")
  orderitem   orderitem[]

  @@index([userId], map: "Order_userId_fkey")
}

model orderitem {
  id          String   @id
  orderId     String
  productId   String
  productName String
  productSku  String?
  productNote String?
  quantity    Float
  createdAt   DateTime @default(now())
  order       order    @relation(fields: [orderId], references: [id], onDelete: Cascade, map: "OrderItem_orderId_fkey")

  @@index([orderId], map: "OrderItem_orderId_fkey")
}

model overtimeleave {
  id            String  @id
  userId        String
  date          String
  type          String
  reason        String  @db.Text
  attachment    String? @db.Text
  requesterName String?
  job           String?
  amount        String?
  adminNote     String? @db.Text
  status        String
  createdAt     String
  updatedAt     String
  user          user    @relation(fields: [userId], references: [id], onDelete: Cascade, map: "OvertimeLeave_userId_fkey")

  @@index([userId], map: "OvertimeLeave_userId_fkey")
}

model payroll {
  id             String        @id
  userId         String
  month          Int
  year           Int
  monthEnc       String        @db.Text
  yearEnc        String        @db.Text
  basicSalaryEnc String        @db.Text
  netSalaryEnc   String        @db.Text
  salarySlipEnc  String?       @db.Text
  createdAt      DateTime      @default(now())
  updatedAt      DateTime
  user           user          @relation(fields: [userId], references: [id], onDelete: Cascade, map: "Payroll_userId_fkey")
  payrollitem    payrollitem[]

  @@unique([userId, month, year], map: "Payroll_userId_month_year_key")
}

model payrollitem {
  id              String          @id
  payrollId       String
  componentId     String
  amountEnc       String          @db.Text
  salarycomponent salaryComponent @relation(fields: [componentId], references: [id], map: "PayrollItem_componentId_fkey")
  payroll         payroll         @relation(fields: [payrollId], references: [id], onDelete: Cascade, map: "PayrollItem_payrollId_fkey")

  @@index([componentId], map: "PayrollItem_componentId_fkey")
  @@index([payrollId], map: "PayrollItem_payrollId_fkey")
}

model product {
  id                String             @id
  name              String
  sku               String?            @unique(map: "Product_sku_key")
  stock             Float              @default(0)
  lowStockThreshold Float              @default(5)
  image             String?
  notes             String?
  drawerLocation    String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime
  recipeingredient  recipeIngredient[]
  transaction       transaction[]
}

model productionIssue {
  @@map("productionissue")
  id               String         @id
  productionUnitId String
  description      String
  resolution       String?
  isResolved       Boolean        @default(false)
  createdAt        DateTime       @default(now())
  updatedAt        DateTime
  productionunit   productionUnit @relation(fields: [productionUnitId], references: [id], onDelete: Cascade, map: "ProductionIssue_productionUnitId_fkey")

  @@index([productionUnitId], map: "ProductionIssue_productionUnitId_fkey")
}

model productionPlan {
  @@map("productionplan")
  id               String           @id
  recipeId         String
  quantity         Float
  month            Int
  year             Int
  status           String           @default("PENDING")
  sectionsSnapshot String?          @db.Text
  createdAt        DateTime         @default(now())
  updatedAt        DateTime
  recipe           recipe           @relation(fields: [recipeId], references: [id], onDelete: Cascade, map: "ProductionPlan_recipeId_fkey")
  productionunit   productionUnit[]

  @@index([recipeId], map: "ProductionPlan_recipeId_fkey")
}

model productionUnit {
  @@map("productionunit")
  id                String            @id
  productionPlanId  String
  unitNumber        Int
  productIdentifier String?
  customId          String?
  completed         String?           @db.Text
  isPacked          Boolean           @default(false)
  isSold            Boolean           @default(false)
  marketplace       String?
  customer          String?
  link              String?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime
  assembledAt       DateTime?
  packedAt          DateTime?
  productionissue   productionIssue[]
  productionplan    productionPlan @relation(fields: [productionPlanId], references: [id], onDelete: Cascade, map: "ProductionUnit_productionPlanId_fkey")

  @@index([productionPlanId], map: "ProductionUnit_productionPlanId_fkey")
}

model project {
  id              String           @id
  name            String
  client          String?
  date            DateTime?
  description     String?          @db.Text
  status          String           @default("PENDING")
  categoryId      String?
  startDate       DateTime?
  endDate         DateTime?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime
  projectcategory projectcategory? @relation(fields: [categoryId], references: [id], map: "Project_categoryId_fkey")
  projectlink     projectlink[]
  user            user[]           @relation("projecttouser")

  @@index([categoryId], map: "Project_categoryId_fkey")
}

model projectcategory {
  id        String    @id
  name      String    @unique(map: "ProjectCategory_name_key")
  createdAt DateTime  @default(now())
  updatedAt DateTime
  project   project[]
}

model projectlink {
  id        String   @id
  label     String
  url       String   @db.Text
  projectId String
  createdAt DateTime @default(now())
  updatedAt DateTime
  project   project  @relation(fields: [projectId], references: [id], onDelete: Cascade, map: "ProjectLink_projectId_fkey")

  @@index([projectId], map: "ProjectLink_projectId_fkey")
}

model rack {
  id            String   @id
  name          String   @unique(map: "Rack_name_key")
  drawerCount   Int      @default(0)
  unusedDrawers Int      @default(0)
  description   String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime
}

model receipt {
  id          String   @id
  date        DateTime @default(now())
  number      String
  name        String
  institution String
  content     String   @db.Text
  link        String?  @db.Text
  filePath    String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime
}

model recipe {
  id               String             @id
  name             String
  productionId     String             @default("-")
  description      String?
  categoryId       String?
  createdAt        DateTime           @default(now())
  updatedAt        DateTime
  productionplan   productionPlan[]
  category         category?          @relation(fields: [categoryId], references: [id], map: "Recipe_categoryId_fkey")
  recipeingredient recipeIngredient[]
  recipesection    recipeSection[]
  transaction      transaction[]

  @@index([categoryId], map: "Recipe_categoryId_fkey")
}

model recipeIngredient {
  @@map("recipeingredient")
  id            String         @id
  recipeId      String
  productId     String
  sectionId     String?
  quantity      Float
  notes         String?
  product       product        @relation(fields: [productId], references: [id], onDelete: Cascade, map: "RecipeIngredient_productId_fkey")
  recipe        recipe         @relation(fields: [recipeId], references: [id], onDelete: Cascade, map: "RecipeIngredient_recipeId_fkey")
  recipesection recipeSection? @relation(fields: [sectionId], references: [id], map: "RecipeIngredient_sectionId_fkey")

  @@index([productId], map: "RecipeIngredient_productId_fkey")
  @@index([recipeId], map: "RecipeIngredient_recipeId_fkey")
  @@index([sectionId], map: "RecipeIngredient_sectionId_fkey")
}

model recipeSection {
  @@map("recipesection")
  id               String             @id
  name             String
  category         String?
  recipeId         String
  order            Int                @default(0)
  createdAt        DateTime           @default(now())
  recipeingredient recipeIngredient[]
  recipe           recipe             @relation(fields: [recipeId], references: [id], onDelete: Cascade, map: "RecipeSection_recipeId_fkey")

  @@index([recipeId], map: "RecipeSection_recipeId_fkey")
}

model replyLetter {
  @@map("replyletter")
  id          String   @id
  date        DateTime @default(now())
  number      String
  name        String
  institution String
  content     String   @db.Text
  link        String?  @db.Text
  filePath    String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime
}

model salaryComponent {
  @@map("salarycomponent")
  id          String        @id
  name        String
  type        String
  createdAt   DateTime      @default(now())
  updatedAt   DateTime
  payrollitem payrollitem[]

  @@unique([name, type], map: "SalaryComponent_name_type_key")
}

model sectioncategory {
  id        String   @id
  name      String   @unique(map: "SectionCategory_name_key")
  createdAt DateTime @default(now())
  updatedAt DateTime
}

model serviceRobot {
  @@map("servicerobot")
  id              String   @id
  entryDate       DateTime @default(now())
  customerName    String
  customerAddress String   @db.Text
  customerPhone   String
  robotType       String
  accessories     String?  @db.Text
  warrantyStatus  String   @default("TIDAK")
  complaint       String   @db.Text
  serviceStatus   String   @default("PENDING")
  serviceNotes    String?  @db.Text
  createdAt       DateTime @default(now())
  updatedAt       DateTime
  image           String?
}

model sparepartProject {
  @@map("sparepartproject")
  id        String   @id
  name      String
  sku       String?
  stock     Float    @default(0)
  notes     String?  @db.Text
  image     String?
  createdAt DateTime @default(now())
  updatedAt DateTime
}

model systemSetting {
  @@map("systemsetting")
  id        String   @id
  key       String   @unique(map: "SystemSetting_key_key")
  value     String   @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime
}

model transaction {
  id          String   @id
  type        String
  quantity    Float
  productId   String?
  recipeId    String?
  userId      String?
  description String?
  createdAt   DateTime @default(now())
  product     product? @relation(fields: [productId], references: [id], onDelete: Cascade, map: "Transaction_productId_fkey")
  recipe      recipe?  @relation(fields: [recipeId], references: [id], map: "Transaction_recipeId_fkey")
  user        user?    @relation(fields: [userId], references: [id], map: "Transaction_userId_fkey")

  @@index([productId], map: "Transaction_productId_fkey")
  @@index([recipeId], map: "Transaction_recipeId_fkey")
  @@index([userId], map: "Transaction_userId_fkey")
}

model user {
  id                                        String            @id
  email                                     String            @unique(map: "User_email_key")
  username                                  String            @unique(map: "User_username_key")
  name                                      String?
  password                                  String
  department                                String?
  role                                      String            @default("USER")
  photoEnc                                  String?           @db.Text
  phoneEnc                                  String?           @db.Text
  addressEnc                                String?           @db.Text
  ktpNumberEnc                              String?           @db.Text
  contractEndDateEnc                        String?           @db.Text
  createdAt                                 DateTime          @default(now())
  updatedAt                                 DateTime
  announcement_announcement_creatorIdTouser announcement[]    @relation("announcement_creatorIdTouser")
  attendance                                attendance[]
  chatmessage                               chatMessage[]
  chatparticipant                           chatParticipant[]
  logactivity                               logActivity[]
  order                                     order[]
  overtimeleave                             overtimeleave[]
  payroll                                   payroll[]
  transaction                               transaction[]
  project                                   project[]         @relation("projecttouser")
  announcement_targetedusers                announcement[]    @relation("targetedusers")
}

model workSchedule {
  @@map("workschedule")
  id        String   @id
  dayOfWeek Int      @unique(map: "WorkSchedule_dayOfWeek_key")
  dayName   String
  startTime String?
  endTime   String?
  isWorkDay Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime
}
